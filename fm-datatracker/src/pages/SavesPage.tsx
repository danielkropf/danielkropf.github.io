import{useState,type FormEvent}from'react';import{useSaves}from'../features/saves/SaveContext';
export function SavesPage(){const{saves,selected,select,create}=useSaves();const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const err=await create({name:String(f.get('name')),club_name:String(f.get('club')),country:String(f.get('country')),game_version:String(f.get('version')),current_season:String(f.get('season'))});setBusy(false);if(err)setError(err);else e.currentTarget.reset()}return <><div className="title-row"><div><span className="eyebrow">LINHAS HISTÓRICAS</span><h1>Saves</h1></div></div><div className="grid"><form className="card form-grid" onSubmit={submit}><h2>Criar save</h2><label>Nome<input name="name" required/></label><label>Clube<input name="club" required/></label><label>País<input name="country"/></label><label>Versão do FM<input name="version" placeholder="FM26"/></label><label>Temporada<input name="season" placeholder="2032/33"/></label><button disabled={busy}>{busy?'Criando…':'Criar save'}</button>{error&&<p className="error">{error}</p>}</form><section className="card"><h2>Seus saves</h2>{saves.length?saves.map(s=><button className={`save-item ${selected?.id===s.id?'selected':''}`} onClick={()=>select(s)} key={s.id}><strong>{s.name}</strong><span>{s.club_name} · {s.current_season||'sem temporada'}</span></button>):<p>Nenhum save criado.</p>}</section></div></>}
