import{describe,expect,it}from'vitest';import{detectImportType,detectNameColumn,normalizeHeader,parseCsv,parseFmDate,prepareRows}from'./importer';describe('importer',()=>{it('não infere data',()=>expect(parseCsv('Name,Club\nAna,X','qualquer.csv')).not.toHaveProperty('snapshotDate'));it('normaliza cabeçalho',()=>expect(normalizeHeader('Decisões ')).toBe('decisoes'));it('detecta squad',()=>expect(detectImportType('export.csv',['Name','Position','Club'])).toBe('squad'));it('normaliza datas do FM',()=>{expect(parseFmDate('16/6/2010')).toBe('2010-06-16');expect(parseFmDate('1.7.2032')).toBe('2032-07-01');expect(parseFmDate('N/A')).toBeNull()});it('preserva e ignora CA/PA',()=>{const r=parseCsv('Name,CA,PA\nAna,99,100','x.csv');expect(r.ignoredColumns).toEqual(['CA','PA']);expect(r.rows[0].CA).toBe('99')});it('prepara o formato real do FM',()=>{const p=parseCsv('Unique ID;Player;Club;Position;Age;DoB;Nation;Expires;Passing\n2002067726;José Lupu;Numantia;GK;15;16/6/2010;Ecuador;N/A;5','Numantia Intake 26.csv');expect(p.delimiter).toBe(';');expect(detectNameColumn(p.headers)).toBe('Player');const r=prepareRows(p,'Player')[0];expect(r.current_name).toBe('José Lupu');expect(r.fm_player_id).toBe('2002067726');expect(r.nationality).toBe('Ecuador');expect(r.date_of_birth).toBe('2010-06-16');expect(r.contract_expiry).toBeNull()})})
