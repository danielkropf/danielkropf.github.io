import{useEffect,useRef,useState}from'react';import{supabase}from'../lib/supabase';import{ATTRIBUTE_CATALOG,DEFAULT_ATTRIBUTE_WEIGHTS,type AttributeCategory}from'../lib/attributes';import{useSaves}from'../features/saves/SaveContext';
type Role={id:string;name:string;weights:Record<string,number>};type Tactic={id:string;name:string;roles:Role[]};export type ModelConfig={general_weights:Record<string,number>;tactics:Tactic[];selected_tactic_id:string|null;selected_role_id:string|null};const categories:Array<{key:AttributeCategory;label:string}>=[{key:'technical',label:'Técnico'},{key:'mental',label:'Mental'},{key:'physical',label:'Físico'},{key:'goalkeeping',label:'Goleiro'}];const freshWeights=()=>({...DEFAULT_ATTRIBUTE_WEIGHTS});const freshConfig=():ModelConfig=>({general_weights:freshWeights(),tactics:[],selected_tactic_id:null,selected_role_id:null});
export function ModelLabPage(){const{selected}=useSaves();const[modelId,setModelId]=useState<string|null>(null),[config,setConfig]=useState<ModelConfig>(freshConfig),[tab,setTab]=useState<AttributeCategory>('technical'),[mode,setMode]=useState<'general'|'role'>('general'),[status,setStatus]=useState('Carregando…'),loaded=useRef(false);useEffect(()=>{loaded.current=false;if(!supabase||!selected)return;void supabase.from('scoring_models').select('id,config').eq('save_id',selected.id).eq('name','Model Lab').order('created_at').limit(1).maybeSingle().then(({data})=>{if(data){const saved=data.config as Partial<ModelConfig>;setModelId(data.id);setConfig({...freshConfig(),...saved,general_weights:{...freshWeights(),...(saved.general_weights??{})}})}else{setModelId(null);setConfig(freshConfig())}loaded.current=true;setStatus('Salvo automaticamente')})},[selected?.id]);useEffect(()=>{if(!loaded.current||!selected||!supabase)return;setStatus('Salvando…');const timer=setTimeout(async()=>{const{data:{user}}=await supabase!.auth.getUser();const payload={owner_id:user!.id,save_id:selected.id,name:'Model Lab',version:'1.0.0',config,is_active:true};const result=modelId?await supabase!.from('scoring_models').update(payload).eq('id',modelId).select('id').single():await supabase!.from('scoring_models').insert(payload).select('id').single();if(result.data?.id)setModelId(result.data.id);setStatus(result.error?`Erro ao salvar: ${result.error.message}`:'Salvo automaticamente')},450);return()=>clearTimeout(timer)},[config,selected?.id,modelId]);const tactic=config.tactics.find(t=>t.id===config.selected_tactic_id),role=tactic?.roles.find(r=>r.id===config.selected_role_id),weights=mode==='role'&&role?role.weights:config.general_weights;function changeWeight(key:string,value:number){if(mode==='general')setConfig(c=>({...c,general_weights:{...c.general_weights,[key]:value}}));else if(tactic&&role)setConfig(c=>({...c,tactics:c.tactics.map(t=>t.id!==tactic.id?t:{...t,roles:t.roles.map(r=>r.id!==role.id?r:{...r,weights:{...r.weights,[key]:value}})})}))}function addTactic(){const name=prompt('Nome da tática (ex.: 4-3-3 Pressão Alta)')?.trim();if(!name)return;const id=crypto.randomUUID();setConfig(c=>({...c,tactics:[...c.tactics,{id,name,roles:[]}],selected_tactic_id:id,selected_role_id:null}));setMode('role')}function addRole(){if(!tactic)return;const name=prompt('Nome da função (ex.: Ball Playing Defender)')?.trim();if(!name)return;const id=crypto.randomUUID();setConfig(c=>({...c,tactics:c.tactics.map(t=>t.id===tactic.id?{...t,roles:[...t.roles,{id,name,weights:freshWeights()}]}:t),selected_role_id:id}));setMode('role')}return <><div className="title-row"><div><span className="eyebrow">MODELOS POR SAVE</span><h1>Model Lab</h1><p>Pesos de 1 a 5. Cada função tática possui um modelo independente.</p></div><span className="save-state">{status}</span></div><section className="card model-toolbar"><button className={mode==='general'?'active':''} onClick={()=>setMode('general')}>Pontuação geral</button><select value={config.selected_tactic_id??''} onChange={e=>{setConfig(c=>({...c,selected_tactic_id:e.target.value||null,selected_role_id:null}));setMode('role')}}><option value="">Selecionar tática</option>{config.tactics.map(t=><option value={t.id} key={t.id}>{t.name}</option>)}</select><button onClick={addTactic}>+ Tática</button><select disabled={!tactic} value={config.selected_role_id??''} onChange={e=>{setConfig(c=>({...c,selected_role_id:e.target.value||null}));setMode('role')}}><option value="">Selecionar função</option>{tactic?.roles.map(r=><option value={r.id} key={r.id}>{r.name}</option>)}</select><button disabled={!tactic} onClick={addRole}>+ Função</button></section><div className="model-tabs">{categories.map(c=><button className={tab===c.key?'active':''} onClick={()=>setTab(c.key)} key={c.key}>{c.label}</button>)}</div>{mode==='role'&&!role?<section className="card empty"><h2>Selecione uma tática e uma função</h2><p>Os pesos desta função serão independentes da pontuação geral.</p></section>:<section className="card sliders"><h2>{mode==='general'?'Pontuação geral':`${tactic?.name} · ${role?.name}`}</h2>{ATTRIBUTE_CATALOG.filter(a=>a.category===tab).map(a=><label className="weight-row" key={a.key}><span>{a.label}</span><input type="range" min="1" max="5" step="1" value={weights[a.key]??1} onChange={e=>changeWeight(a.key,Number(e.target.value))}/><output>{weights[a.key]??1}</output></label>)}</section>}</>}
