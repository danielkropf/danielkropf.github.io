import{describe,expect,it}from'vitest';import{ATTRIBUTE_CATALOG,DEFAULT_ATTRIBUTE_WEIGHTS}from'./attributes';describe('catálogo de atributos',()=>{it('possui as quatro categorias',()=>expect(new Set(ATTRIBUTE_CATALOG.map(a=>a.category))).toEqual(new Set(['technical','mental','physical','goalkeeping'])));it('inicia todos os pesos em 3',()=>expect(Object.values(DEFAULT_ATTRIBUTE_WEIGHTS).every(v=>v===3)).toBe(true))})
