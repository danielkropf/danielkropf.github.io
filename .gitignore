# -----------------------------------------------------------------------------
# Local-only project material
# -----------------------------------------------------------------------------
# Documentation/handoff and reverse-engineering workspaces are useful locally,
# but are not part of the website delivered by GitHub Pages.
/docs/
/tools/
/fm-datatracker/docs/

# Private FM reader / Oracle validation material.
/fm-datatracker/src/lib/fm26-offline-reader.private-fixtures.test.ts
/fm-datatracker/supabase/migrations/*_oracle_*.sql

# Local agent/instruction files.
/AGENTS.md

# A package lock in the repository root is not used. The lockfile inside
# fm-datatracker MUST remain versioned because GitHub Actions uses npm ci.
/package-lock.json

# -----------------------------------------------------------------------------
# OS / editor / temporary files
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
*.tmp
