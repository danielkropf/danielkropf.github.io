import{useEffect,useState}from'react';import{supabase}from'../lib/supabase';import{useSaves}from'../features/saves/SaveContext';import{ImportPanel}from'../features/imports/ImportPanel';import type{ImportRecord}from'../types/domain';export function ImportsPage(){const{selected}=useSaves();const[items,setItems]=useState<ImportRecord[]>([]);async function load(){if(!supabase||!selected)return;const{data}=await supabase.from('imports').select('*').eq('save_id',selected.id).order('created_at',{ascending:false});setItems((data??[])as ImportRecord[])}useEffect(()=>{void load()},[selected?.id]);return <><ImportPanel onImported={()=>void load()}/><section className="section"><h2>Histórico de imports</h2><div className="table-wrap"><table><thead><tr><th>Data</th><th>Arquivo</th><th>Tipo</th><th>Linhas</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.snapshot_date}</td><td>{i.original_filename}</td><td>{i.file_type}</td><td>{i.row_count}</td><td><span className="status">{i.status}</span></td></tr>)}</tbody></table></div>{!items.length&&<p>Nenhum import confirmado.</p>}</section></>}
