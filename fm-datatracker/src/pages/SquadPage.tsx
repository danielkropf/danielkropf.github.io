import{useEffect,useMemo,useState}from'react';import{supabase}from'../lib/supabase';import{attributeScore}from'../lib/scoring';import{useSaves}from'../features/saves/SaveContext';import type{PlayerRow}from'../types/domain';
const weights:Record<string,number>={passing:8,technique:7,decisions:9,composure:8,positioning:8,anticipation:8,pace:6,acceleration:6,strength:6,stamina:6,tackling:7,finishing:7};
export function SquadPage(){const{selected}=useSaves();const[players,setPlayers]=useState<PlayerRow[]>([]),[chosen,setChosen]=useState<PlayerRow|null>(null),[search,setSearch]=useState('');useEffect(()=>{if(!supabase||!selected)return;void supabase.from('players').select('id,current_name,nationality,last_seen_date,is_active,player_snapshots(id,snapshot_date,age,club,squad,positions,contract_expiry,player_attributes(attribute_key,attribute_label,value,category))').eq('save_id',selected.id).order('current_name').then(({data})=>setPlayers((data??[])as unknown as PlayerRow[]))},[selected?.id]);const filtered=useMemo(()=>players.filter(p=>p.current_name.toLowerCase().includes(search.toLowerCase())),[players,search]);return <><div className="title-row"><div><span className="eyebrow">ELENCO</span><h1>{selected?.club_name}</h1><p>{players.length} jogadores históricos</p></div><input className="search" placeholder="Buscar jogador" value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="table-wrap"><table><thead><tr><th>Jogador</th><th>Equipe</th><th>Posições</th><th>Último snapshot</th><th>Nota atributos</th></tr></thead><tbody>{filtered.map(p=>{const snaps=[...p.player_snapshots].sort((a,b)=>b.snapshot_date.localeCompare(a.snapshot_date)),s=snaps[0],score=s?attributeScore(s.player_attributes.map(a=>({key:a.attribute_key,value:a.value,weight:weights[a.attribute_key]??1}))):null;return <tr key={p.id} onClick={()=>setChosen(p)} className="clickable"><td><strong>{p.current_name}</strong><small>{p.nationality}</small></td><td>{s?.club||s?.squad||'—'}</td><td>{s?.positions?.join(', ')||'—'}</td><td>{s?.snapshot_date||'—'}</td><td><b>{score===null?'—':score.toFixed(1)}</b></td></tr>})}</tbody></table></div>{chosen&&<PlayerDrawer player={chosen} close={()=>setChosen(null)}/>}</>}
function PlayerDrawer({player,close}:{player:PlayerRow;close:()=>void}){const snaps=[...player.player_snapshots].sort((a,b)=>b.snapshot_date.localeCompare(a.snapshot_date));return <div className="overlay" onClick={close}><article className="drawer" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><span className="eyebrow">FICHA HISTÓRICA</span><h1>{player.current_name}</h1>{snaps.map((s,index)=>{const prev=snaps[index+1];return <section className="snapshot" key={s.id}><h3>{s.snapshot_date} · {s.club||s.squad||'Sem equipe'}</h3><div className="attribute-grid">{s.player_attributes.sort((a,b)=>b.value-a.value).map(a=>{const old=prev?.player_attributes.find(x=>x.attribute_key===a.attribute_key)?.value,delta=old===undefined?null:a.value-old;return <div key={a.attribute_key}><span>{a.attribute_label}</span><b>{a.value}</b>{delta!==null&&<small className={delta>0?'up':delta<0?'down':''}>{delta>0?'+':''}{delta}</small>}</div>})}</div></section>})}{!snaps.length&&<p>Jogador sem snapshot de atributos.</p>}</article></div>}
