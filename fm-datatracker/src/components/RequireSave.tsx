import type{ReactNode}from'react';import{Link}from'react-router-dom';import{useSaves}from'../features/saves/SaveContext';export function RequireSave({children}:{children:ReactNode}){const{selected,loading}=useSaves();if(loading)return<p>Carregando…</p>;if(!selected)return <section className="card empty"><h2>Crie seu primeiro save</h2><p>Imports, elenco e análises sempre pertencem a uma linha histórica.</p><Link className="button" to="/saves">Criar save</Link></section>;return children}
