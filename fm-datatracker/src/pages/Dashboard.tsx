import { ImportPanel } from '../features/imports/ImportPanel'
import { useSaves } from '../features/saves/SaveContext'

export function Dashboard() { const { selected, saves } = useSaves(); return <><header><div><span className="eyebrow">BANCO MESTRE</span><h1>{selected?.name ?? 'Bem-vindo ao FM DataTracker'}</h1><p>{selected ? `${selected.club_name} · ${selected.current_season ?? 'Temporada não informada'}` : 'Crie seu primeiro save para começar uma linha histórica independente.'}</p></div><div className="save-count"><strong>{saves.length}</strong><span>saves ativos</span></div></header><div className="grid"><section className="card hero"><span className="eyebrow">VISÃO GERAL</span><h2>Histórico antes de tudo.</h2><p>Cada export vira uma fotografia imutável. A evolução, os contratos e as notas permanecem auditáveis temporada após temporada.</p><div className="mini-grid"><div><b>0</b><span>Jogadores</span></div><div><b>0</b><span>Snapshots</span></div><div><b>—</b><span>Último import</span></div></div></section><section className="card"><span className="eyebrow">QUALIDADE</span><h2>Pronto para validar</h2><ul><li>CA/PA ignorados no scoring</li><li>Datas Numancia reconhecidas</li><li>Duplicidade por hash planejada</li></ul></section></div><ImportPanel/></> }
